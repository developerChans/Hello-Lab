import { withRouter } from 'react-router-dom';
import LabPage from '../LabPage';

const LabInfo = () =>{
    return (
        <div>
            <LabPage/>
        </div>
    );
}
export default withRouter(LabInfo);
